// src/ts/pages/show.ts
import "../components/show/ShowSection";

function initializeShowPage(): void {
  console.log("Show Page charged");
  // Scripts spécifiques à la page film
}

window.addEventListener("DOMContentLoaded", initializeShowPage);
